import React, { Component } from 'react'

export default class Header extends Component {
  render() {
    return (
      <header>
        Header
      </header>
    )
  }
}

// When button is clicks
// Should render a shopping cart component instead of the main component as a route